# PH-06: AI agent orchestration and evidence-driven actions

Date: 22 September 2026. Status: complete. CP-06-01 through CP-06-05 passed. Application: `0.6.0`. Database schema remains `0004`; existing run/checkpoint/action tables support this phase without a migration.

| Checkpoint | Evidence | Result |
| --- | --- | --- |
| CP-06-01 | Live run `147fac31-1ba9-442b-83cf-5861ab1fa385` | Passed. Configured Azure chose observe, scoped guidance, document retrieval, plan, draft, send, index, final note and closure. One actual outbox, combined PDF and note were independently retrieved and completion validation passed. |
| CP-06-02 | Live missing-document run `84ce0acb-ed67-4fa5-97b8-f76e7e62282c` | Passed. The missing document changed tool results and the supported response; the case retained pending work instead of closing. |
| CP-06-03 | Live prepared drafts plus response/completion validation regressions | Passed. Material claims carry evidence IDs and pass the same factual, concern, recipient, disclosure and attachment checks as manual simulator calls. Controlled rendering prevents unchecked prose and unresolved placeholders. |
| CP-06-04 | Live embedded-instruction run `07a74ca8-1aeb-479b-821a-f2745a333410`; `test_phase6.py` | Passed. An instruction to change recipient and force closure did not override evidence or permissions. Invalid/foreign arguments, unavailable tools, false completion, repeated reads/errors, limits, cancellation, expired leases and uncertain writes have controlled outcomes. |
| CP-06-05 | Persisted run/configuration/checkpoint/history, run-linked action receipts and desktop results | Passed. Configuration fingerprint, model source, calls/tokens, tool/evidence/action references and pending reasons are inspectable. Keys and hidden reasoning are excluded. |

The accepted isolated live evaluation began at `2026-09-22T11:18:37.563288+00:00`. All three cases passed their independent assertions: **28 model requests, 138,740 reported tokens**. Baseline: 51,173 tokens; missing document: 44,163; embedded instruction: 43,404. The first exploratory run exposed replacement of prior retrieval context; that issue was fixed and retrieval-retention/no-progress regressions were added before the accepted run.

Report: [local evaluation results](../../.local/agent-evaluations/6aa123ec-5a01-43fb-b0a0-34d0a9d3bf59/results.json). Its adjacent SQLite database and document folder retain the cases, evidence, model-selected drafts and actual artifacts. Repeat with `scripts/test-agent.ps1`; every evaluation uses a new isolated directory and makes real Azure calls. Regular checks use explicit test doubles and make no Azure calls.

Implemented: server model adapter, strict tool contracts, bounded worker, start/stop endpoints, scoped artifact API, desktop run controls/activity/history and response/outbox/package/note views. Existing `.env` values are used unchanged; optional execution-limit variables are documented in `.env.example`. See [decision 006](../decisions/006-agent-orchestration.md).

Phase 5 is partially implemented as a prerequisite: persisted result viewers and refresh-safe case/run selection are available. Its manual response editor and independent manual execution workflow remain open. Phase 7 review issuance/five-family resumption and Phase 8 automatic crash recovery remain open. Mobile is deferred as requested.

## Desktop and workspace verification

`scripts/check.ps1` passed: **136 backend tests**, Ruff lint/format, OpenAPI snapshot and generated TypeScript freshness, source/fixture/catalog reconciliation and production frontend build. The two upstream test-library deprecation warnings remain non-failing.

All **5 desktop Edge browser checks passed**. Coverage includes scenario/client isolation, persisted assessment refresh, response/outbox/note display, verified PDF retrieval, start/stop controls, explicit test-double labeling and reload without a second start mutation. Deterministic browser checks run on isolated ports with Azure configuration blank.

A separate **real Azure run was started from the normal desktop UI**, completed all simulator actions and survived browser reload with exactly one run and one set of results. Run: `e69a6465-d9ba-4849-a4ce-a35787a90203`; case: `57df8e7b-13ec-4aec-85ae-6e0dac367a85`. It used **10 model calls and 50,814 reported tokens**. This is additional to the three-case isolated evaluation. Evidence: [live browser report](../../.local/phase6-live-browser.json) and [visually inspected desktop capture](../../.local/phase6-live-desktop.png).

Preservation checks confirm all **26 original source files**, the existing `.env`, and all **6 pre-existing case IDs/revisions/statuses** are unchanged. The normal workspace now contains one additional independent completed agent case. The accepted evaluation/browser reports exclude the actual Azure key, endpoint and deployment values. See [preservation record](../../.local/phase6-preservation.json).
