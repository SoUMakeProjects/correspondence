# PH-09: Acceptance, packaging and demo handover

Date: 22 September 2026. Status: complete for handover of the implemented local synthetic demo. Application `0.9.0`; schema `0005`. This does **not** mark the incomplete PH-05 manual workflow or every required BRD behavior complete.

| Checkpoint | Result and evidence |
| --- | --- |
| CP-09-01 | Passed. `scripts/check.ps1 -Browser`: 166 backend tests and all 10 desktop Edge tests, lint/format, OpenAPI snapshot, generated type freshness, source/fixture reconciliation and frontend build. The fresh bundle also installs and builds independently. |
| CP-09-02 | Passed as an acceptance-reporting gate. All 52 IDs have a disposition and evidence reference in the [ledger](../acceptance_results.md). 26 of 30 required items pass at recorded scope; AC-03, AC-04, AC-16 and AC-45 remain partial. Three planned partial items pass selected subsets; 19 deferred items are not reported as run. |
| CP-09-03 | Passed for evaluated cases. All five families and both live recovery variants reach verified outcomes. Delivered claims, recipient/client identity, reviewed versions, single-task/delivery behavior, closure and intentional pending/transfer outcomes were checked. Rejected draft proposals remain in the history; no unresolved critical defect was found in the evaluated delivered results. |
| CP-09-04 | Passed. A new directory extracted from the runtime bundle follows setup, seed, build and alternate-port startup instructions, then completes a live Azure browser case. Reload and actual backend restart retain the same run and artifacts. No database edits or private source files are needed. |
| CP-09-05 | Passed. [Runbook](../Demo_Runbook.md), [handover](../Handover.md), [remaining work](../Follow_ups.md), [package instructions](../Package_Readme.md), this evidence and [decision 009](../decisions/009-demo-handover.md) record scope, limits, checks, restart and replay. |

## Final engineering checks

The complete offline command passed **166 backend tests in 154.48 seconds** and **10 browser tests in 2.1 minutes**. Two existing test-library deprecation warnings remain non-failing. Catalog checks reconcile 149 taxonomy identities, 527 source rows, 15 curated items, two clients and 12 scenario/variant combinations. Document fixtures retain 15 available, two missing and one deliberately unreadable reference.

Phase 9 changes release metadata and setup status wording, adds the allowlisted bundle helper, and finalizes evidence/guides. Business algorithms, model protocol, tool schemas and database schema are unchanged. The final wording-only UI refinement and bundle source both passed the frontend build. The package helper passes lint/format. No new environment values were required.

## Live Azure evaluations

The [Phase 9 live index](../../.local/phase9-live.json) records **15 runs, 112 model requests and 777,179 reported tokens** across the workflow and recovery evaluations. Run configuration retains its source, fingerprint, protocol and limits; no endpoint/deployment/key values are included.

The [five-family report](../../.local/workflow-evaluations/5c78b904-1057-442e-b558-e5993863d296/results.json) passed all five cases: 11 runs, 88 requests, 647,647 reported tokens.

| Case | Observed outcome |
| --- | --- |
| DEMO-01 | Specific request for missing legal evidence; same-case supplied document; one task and actual name update before confirmation; two retained responses/packages/notes. The unmapped classification retains a supervisory handoff and the case waits on department. |
| DEMO-02 | Available schedule retrieved, validated response sent, indexed, noted and closed. One delivery/package/note. |
| DEMO-03 | Client review pause; supplied Tax Team result invalidates old draft; edited current version approved and then sent. Existing task reused. Scheduled is never represented as paid; case closes. |
| DEMO-04 | Conflicting evidence produces a handoff. Supplied determination is inspected, representative restriction retained, current handoff acknowledged, case transferred. No false closure. |
| DEMO-05 | Initial EFT-purpose clarification request; outgoing-refund answer produces a specific authorization requirement. Case remains waiting for borrower; no funds movement. |

The model made some invalid tax/EFT draft proposals. Validation rejected them, the model corrected them, and only validated versions reached delivery. This is retained evidence of the guard working, not an omitted failure. These runs needed no provider-error retry.

The [recovery report](../../.local/recovery-evaluations/1fc7cf00-7045-43e6-b878-51c0228ba0c7/results.json) passed both variants: four runs, 24 requests, 129,532 reported tokens. Indexing failure resumes missing records without resending. Lost send result with unavailable status remains blocked until explicit status restoration and independent verification; the resumed agent reuses the original delivery. Each case ends with one delivery/package/note and valid completion.

## Response and PDF review

[Content audit](../../.local/phase9-content-review.json) verifies all six delivered response packages, seven PDF pages including the schedule attachment, against actual case/loan/client/draft/outbox records and stored file hashes. Recipient addresses are synthetic `example.com` destinations; client disclosure IDs match the case configuration. Selected-run reference exports exclude servicing identifiers and configured Azure values.

The [six-response overview](../../.local/phase9-content/overview.png), [amortization attachment](../../.local/phase9-content/DEMO-02-1-2.png) and [tax response](../../.local/phase9-content/DEMO-03-1-1.png) were visually inspected. Text is readable, amounts/dates/identities are consistent and no clipping was observed. Wording is intentionally structured and sometimes repetitive; polishing repeated labels and ISO timestamps is recorded as follow-up, not as a fact-validation defect. The bankruptcy case uses a recorded specialist handoff rather than an unnecessary outgoing PDF.

## Bundle and clean-instance rehearsal

`scripts/package-demo.ps1` writes a new archive with an internal SHA-256 manifest. The [bundle record](../../.local/phase9-package.json) identifies the final ZIP/hash. It excludes credentials, private Office sources and their path registry, previous runtime data and development tests. Original-source maintenance checks remain in the development workspace; startup/seed/build require only sanitized runtime inputs.

The rehearsal extracted into `.local/p9-rehearsal`, created a fresh Python environment, installed locked Python/Node dependencies through `setup.ps1`, migrated from an empty database and built the frontend. Node installation reported zero audit vulnerabilities at that time. Seeding twice retained five cases. The backend was configured through process environment values for the test; the extracted `.env` remained placeholder-only.

Following the runbook on ports 8023/5175, the browser created a sixth case and drove run `aa6318ec-294d-42ce-9453-50c156bf9592` to automatic closure using live Azure. It used **10 additional requests and 62,538 reported tokens**. Export and browser reload retained one run and one delivery/package/note. Both rehearsal processes were stopped, and an actual backend restart retained those same records. All temporary rehearsal servers were then stopped.

Evidence: [installation/start/restart measurements](../../.local/phase9-rehearsal.json), [live browser report](../../.local/phase9-live-browser.json), [workspace capture](../../.local/phase9-rehearsal-workspace.png) and [completed-run summary](../../.local/phase9-rehearsal-summary.png). Total Phase 9 live usage including this separate browser case is **16 runs, 122 requests and 839,717 reported tokens**.

The final bundle's application files match the rehearsed files. Guides and environment-example comments were finalized afterward; `.env` values were never copied into the bundle. The isolated archive/setup exercise also caught and corrected an initial packaging assumption about unused TypeScript config filenames before the accepted bundle was built.

## Observed local timings and preservation

Hardware: Lenovo 21M4S5YG00, AMD Ryzen 5 7535U, six cores / 12 logical processors, approximately 14.8 GiB RAM; Windows, Python 3.14, Node 25.2.1 and desktop Edge. Model configuration is the redacted fingerprint in the live index, using the existing Azure OpenAI v1 adapter.

Backend first readiness including launcher: 14.56 seconds; frontend readiness: 5.54 seconds; backend restart: 11.74 seconds. Browser first workspace render: 735 ms; reload to retained case: 316 ms. Ten worklist API reads measured 9–14 ms (median 11 ms). Fifteen evaluation runs measured 12.22–75.73 seconds each (median 30.87); the fresh-bundle browser case took 53.86 seconds from click to visible closure. These are small local samples, exclude install downloads/human waits, and are not production SLAs.

[Preservation audit](../../.local/phase9-preservation.json) records unchanged original sources, credential file and pre-existing case records. All new evaluation cases are isolated from the normal workspace. The normal desktop is returned to port 5173 with API version `0.9.0` and schema `0005`.

PH-05 and the four required acceptance gaps remain open. Mobile and SSE are deferred; the desktop polls persisted state. Real-system integration, production identity/deployment and source-policy signoffs remain separate work. The unchecked broader MVP completion criteria are not overridden by this handover checkpoint.
