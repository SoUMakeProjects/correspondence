# PH-02: Synthetic fixtures and curated source knowledge

Date: 22 September 2026. Status: complete. CP-02-01 through CP-02-05 passed.

Implemented five versioned synthetic scenarios, two fictional client configurations, prepared follow-ups, a PDF library and deliberate document exceptions, the complete taxonomy/source-row manifest, scoped curated retrieval, schema migration `0002`, idempotent seed helpers, and browser controls for loading/inspecting the data.

| Checkpoint | Evidence | Result |
| --- | --- | --- |
| CP-02-01 | Five cases, valid references, synthetic identity/amount/date data, isolated follow-ups | Passed. All five initial scenarios and 12 scenario/variant combinations validate. API tests verify scoped evidence/tasks/concerns and exclusion of future results from initial inputs. Both fictional client configurations load. |
| CP-02-02 | 149 parent-qualified classifications; repeated labels retained | Passed. All 149 original triples are retained, with 144 distinct subclass labels. Scenario-family tags remain separate, and cases do not invent an operational classification. |
| CP-02-03 | 527 rows reconciled; credential/contact exclusions before runtime serialization | Passed. Physical rows 2–528 across 16 categories are accounted for: 5 curated, 10 excluded, 512 requiring review. The historical 525 count and 14 repeated-label groups are recorded. Runtime metadata contains no source response/subcategory text or credential values. |
| CP-02-04 | Actual readable PDFs and identifiable missing/unreadable/wrong-loan variants | Passed. All 15 available PDFs parse with the expected metadata; 2 references have no file and 1 deliberately truncated file fails parsing. Scoped download tests verify missing, unreadable, wrong-loan, cross-case, and changed-file behavior. Rendered documents were inspected visually, including the complete one-page amortization schedule and tax bill. |
| CP-02-05 | Scenario/client-scoped retrieval with provenance; source-review content withheld | Passed. Fifteen versioned demo items expose source references, applicability, prerequisites, and limitations. Tests verify text/kind filtering, client disclosure separation, and removal from retrieval when an item requires review. No draft API consumes unreviewed source text. |

See [data guide](../../data/README.md), [reconciliation report](../../data/knowledge/reconciliation.v1.json), [tests](../../backend/tests/test_phase2.py), and [evaluation expectations](../../backend/tests/expectations/v1/scenario_outcomes.json).

## Verification results

- Backend: **35 tests passed** in 32.10 seconds, covering the original foundation and new fixture/catalog/document behavior. This includes upgrading a populated schema `0001` database while preserving its case, event and evidence, and rolling back records/files after a simulated disk-write failure.
- Data: `python -m app.cli check-data` passed source reconciliation, fixture references, template placeholders, client disclosures, document identities/readability/hashes, and source-reference checks.
- Contracts/build: Ruff lint/format, backend OpenAPI snapshot comparison, generated TypeScript freshness, and TypeScript/Vite production build passed.
- Browser: **4 checks passed** in 52.8 seconds across desktop and mobile Chromium emulation in installed Microsoft Edge. Both the foundation workflow and scenario loading/PDF retrieval/missing evidence/curated provenance workflows passed. Desktop/mobile screenshots were inspected. This does not claim actual Safari/iOS coverage.
- Local startup: backend restarted successfully on port 8000 and Vite serves the updated UI on port 5173. Proxied health reports version `0.2.0` and migration `0002`; scenario and reference endpoints return HTTP 200. The documented seed helper loaded all five initial cases; repeating it returned the same cases without duplicate events.
- Preservation: SHA-256 comparison confirms all **26 original source files are unchanged**. Existing `.env` was preserved; no Azure/model call was made. New data and application files contain synthetic inputs and curated content only.

Validation commands: `scripts/check.ps1 -Browser` ran the backend/data/contract/build checks; after correcting browser selectors for summary labels and shared citations, `npm --prefix frontend run test:e2e` passed all four browser checks. The two existing upstream Python test-library deprecation warnings remain non-failing.

The initial dataset contains five independent simulation instances, each at fixture version 1 and the fixed Eastern evaluation clock. No agent run, simulator action receipt, outbox entry, or closure was created. Expected business outcomes remain evaluation-only data, separate from application inputs.

Implementation decisions are recorded in [the Phase 2 decision record](../decisions/002-synthetic-data.md). AC-38's import/reconciliation criteria are now supported; the remaining business scenarios require their later-phase workflow demonstrations.

Scope limits: prepared follow-ups are alternative starting snapshots, not a completed resume workflow. The app does not yet enforce routing/aging, draft responses, execute simulator actions, or invoke Azure. All production policy gaps remain explicit. PH-03 has not started.
