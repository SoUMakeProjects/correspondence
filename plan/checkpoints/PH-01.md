# PH-01: Application foundation and persisted contracts

Date: 22 September 2026. Status: complete. CP-01-01 through CP-01-05 passed.

Implemented: React worklist, FastAPI endpoints, 15 application tables and Alembic migration, UTC/text/integer data contracts, transactional case updates/events, idempotent mutation receipts, optimistic revisions, persisted worker ownership primitives, redacted Azure status, environment templates, and Windows helpers.

| Checkpoint | Required evidence | Current result |
| --- | --- | --- |
| CP-01-01 | Local Windows UI/API startup | Passed. Both documented startup scripts ran successfully. UI, direct/proxied API health, and proxied Swagger documentation returned HTTP 200 on ports 5173/8000. |
| CP-01-02 | API-created case/event survives process restart | Passed. The subprocess test creates a case through HTTP, terminates the server, restarts against the same database, and compares the saved case and event history. Browser reload also preserves the owner, state, and history. |
| CP-01-03 | Identifier/time/amount preservation | Passed. Leading-zero loan/CCID strings, integer monetary amounts, and an offset-aware timestamp at the Eastern DST boundary round-trip as the same instant in UTC, including microseconds. Invalid numeric/coerced amounts and naive timestamps are rejected. |
| CP-01-04 | Invalid/stale updates cause no partial mutation | Passed. Invalid input, unavailable transitions, stale revisions, reused mutation IDs with changed payloads, and conflicting loan/case creation leave no partial writes. Replaying an identical mutation returns its saved result without another event. |
| CP-01-05 | Build/types, contract, tests, and ignored secrets/runtime data | Passed. Ruff lint/format, 18 backend tests, OpenAPI snapshot comparison, generated TypeScript comparison, frontend production build/typecheck, and 2 browser checks passed. Ignore patterns cover environment secrets, databases, dependencies, and runtime/test output. No Git repository has been initialized, so this verifies ignore configuration rather than tracked-file state. |

## Verification evidence

The following complete check command exited successfully:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1 -Browser
```

- Backend: **18 passed** in 18.74 seconds, including actual process restart, transaction rollback, idempotency, event scoping, config redaction, and worker lease fencing. See [foundation tests](../../backend/tests/test_foundation.py) and [process restart test](../../backend/tests/test_process_restart.py).
- Contracts/build: backend OpenAPI matches [the snapshot](../../backend/openapi.json); generated frontend types match that snapshot; TypeScript and Vite production build passed.
- Browser: **2 passed** in 31.2 seconds using installed Microsoft Edge, at desktop and Chromium mobile-emulation sizes. Each verifies sample creation, owner editing, queue-to-review transition, reload persistence, setup status, and viewport overflow. See [browser workflow](../../frontend/tests/e2e/foundation.spec.ts). This is not an actual iOS/Safari test.
- Visual review: desktop and mobile screenshots were inspected. The worklist scrolls horizontally within its table on small screens; details and activity remain within the screen width. Screenshots are local artifacts under `frontend/test-results/`.
- Startup: [backend](../../scripts/start-backend.ps1) and [frontend](../../scripts/start-frontend.ps1) helpers successfully served the normal workspace. Health returned schema revision `0001`; direct API health, proxied health, UI, and API documentation all returned HTTP 200.
- Documentation/configuration: all local Markdown link targets in the README and `plan/` resolve. All 26 original source files remain under `docs/`. Both environment files exist, and the existing `.env` was preserved.

Environment: Python 3.14.0, Node 25.2.1, npm 11.6.2, Windows, local SQLite. Exact package versions are recorded in the backend/frontend lock files. Model provider selected: Azure Foundry OpenAI; no model calls made.

## Repairs and phase limits

The mobile browser check exposed an absolutely positioned screen-reader label escaping the table's scroll container and expanding the layout viewport. The container now establishes the positioning boundary, mobile owner inputs use 16px text, and the browser assertion compares the document/layout width with the configured screen width. The save interaction passes without forced clicks. Explicit refresh/retry also clears a previous UI error.

Phase limits: full scenario fixtures, aging/business rules, simulator operations, streaming progress, AI orchestration, and response approvals remain in their planned later phases. Reserved run/review endpoints return `501`. Queue/review state changes are the only available case transitions.

The interrupted session left a pre-existing system pytest temp directory with incompatible Windows permissions. Tests now use a fresh workspace-local directory per invocation. Two upstream test-library deprecation warnings remain; they do not fail the checks.

Next planned phase: PH-02 synthetic fixtures and curated knowledge. It has not started. Azure connectivity and genuine model execution remain PH-06 work; broader business acceptance remains unrun in [the acceptance ledger](../acceptance_results.md).
